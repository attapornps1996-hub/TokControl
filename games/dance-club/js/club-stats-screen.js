/**
 * Club stats HUD — disabled (LED wall is the only stage display).
 */
export function createClubStatsScreen() {
    return {
        setStats() {},
        update() {},
        dispose() {},
        meshes: []
    };
}
